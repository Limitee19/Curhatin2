# 🧠 AI Mental Health Support Chatbot (Curhatin)

Chat anonim dengan AI empatik + deteksi risiko + rekomendasi self-care.

## ✅ Fitur
- Anonim (tanpa PII)
- Deteksi emosi (HuggingFace)
- Deteksi risiko bunuh diri
- Rekomendasi self-care
- Dashboard admin (tren agregat)
- Siap Docker

## 🚀 Jalankan Lokal

1. Buat virtual env:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   .\venv\Scripts\Activate.ps1  # Windows (PowerShell)