# docker-compose.yml
version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - AI_PROVIDER=gemini
      - GEMINI_API_KEY=AIzaSyB4c3bYfUzz9572-PDrAQmTvd5aA2TWaiY
    volumes:
      - ./logs:/app/logs
    command: python manage.py runserver 0.0.0.0:8000